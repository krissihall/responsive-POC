import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:checkout/cart-details', 'CheckoutCartDetailsView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
