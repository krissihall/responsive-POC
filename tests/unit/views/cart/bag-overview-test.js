import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:cart/bag-overview', 'BagOverviewView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
